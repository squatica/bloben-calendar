export const setContextMock = (key: string, value: any) => {
  return {
    key,
    value,
  };
};

export const setUseStateMock = (value: any) => {
  return value;
};

export const toastMock = (item: any) => {
  return item;
};
